# Keep parity with Home Assistant integration constraints:
# 4.10.0.84 and 4.11.0.86 are excluded because they have known runtime issues in this project.
opencv-python-headless>=4.5.0,!=4.10.0.84,!=4.11.0.86
pytesseract>=0.3.10
Pillow>=9.0.0
